services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: lolmysite
      POSTGRES_USER: lolmysite
      POSTGRES_PASSWORD: lolmysite
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lolmysite"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      DJANGO_DEBUG: "True"
      DJANGO_SECRET_KEY: dev-insecure-secret-key-change-me
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1,0.0.0.0
      DATABASE_URL: postgres://lolmysite:lolmysite@db:5432/lolmysite
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
