# Render blueprint (M7). Deploy with: Render Dashboard → New → Blueprint.
# Secrets marked `sync: false` must be filled in the Render dashboard.

envVarGroups:
  - name: lolmysite-shared
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_MANIFEST_STATIC
        value: "True"
      - key: PYTHON_VERSION
        value: "3.12.7"
      - key: RIOT_PLATFORM
        value: jp1
      - key: RIOT_REGIONAL
        value: asia
      - key: DISCORD_CLIENT_ID
        sync: false
      - key: DISCORD_CLIENT_SECRET
        sync: false
      - key: RIOT_API_KEY
        sync: false
      - key: SENTRY_DSN
        sync: false

databases:
  - name: lolmysite-db
    databaseName: lolmysite
    user: lolmysite
    plan: free

services:
  - type: redis
    name: lolmysite-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []  # internal only

  - type: web
    name: lolmysite
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    preDeployCommand: "python manage.py migrate --noinput"
    startCommand: "gunicorn config.wsgi:application"
    healthCheckPath: /healthz
    envVars:
      - fromGroup: lolmysite-shared
      - key: DJANGO_ALLOWED_HOSTS
        sync: false  # e.g. lolmysite.onrender.com
      - key: DJANGO_CSRF_TRUSTED_ORIGINS
        sync: false  # e.g. https://lolmysite.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: lolmysite-db
          property: connectionString
      - key: CACHE_URL
        fromService:
          type: redis
          name: lolmysite-redis
          property: connectionString

  # F-REC-05: expire recruitments past their start time, every minute.
  - type: cron
    name: lolmysite-expire-recruitments
    runtime: python
    plan: free
    schedule: "* * * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py expire_recruitments"
    envVars:
      - fromGroup: lolmysite-shared
      - key: DATABASE_URL
        fromDatabase:
          name: lolmysite-db
          property: connectionString
      - key: CACHE_URL
        fromService:
          type: redis
          name: lolmysite-redis
          property: connectionString

  # F-ACC-08: refresh active users' ranks daily (03:00 JST = 18:00 UTC).
  - type: cron
    name: lolmysite-refresh-ranks
    runtime: python
    plan: free
    schedule: "0 18 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python manage.py refresh_ranks"
    envVars:
      - fromGroup: lolmysite-shared
      - key: DATABASE_URL
        fromDatabase:
          name: lolmysite-db
          property: connectionString
      - key: CACHE_URL
        fromService:
          type: redis
          name: lolmysite-redis
          property: connectionString
